import {Label} from "kb2abot/util/logger"

export const remoteHook = new Label("REMOTE HOOK", ["yellow"])
export const remoteServer = new Label("REMOTE SERVER", ["yellow"])
export const request = new Label("REQUEST", ["yellow"])
export const response = new Label("RESPONSE", ["blue"])